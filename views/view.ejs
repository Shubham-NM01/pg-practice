<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>View PDF - <%= pdf.originalFilename %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <h1><%= pdf.originalFilename %></h1>
    <div class="pdf-info">
      <p><strong>Description:</strong> <%= pdf.description || 'No description provided' %></p>
      <p><strong>Uploaded:</strong> <%= new Date(pdf.uploadDate).toLocaleString() %></p>
    </div>
    <div class="actions">
      <a href="/" class="btn">Back to Home</a>
      <a href="/edit/<%= pdf.id %>" class="btn">Edit</a>
      <a href="/download/<%= pdf.id %>" class="btn">Download</a>
      <a href="/delete/<%= pdf.id %>" class="btn delete" onclick="return confirm('Are you sure you want to delete this PDF?')">Delete</a>
    </div>
    
    <div class="pdf-viewer">
      <embed src="/uploads/<%= pdf.filename %>" type="application/pdf" width="100%" height="600px" />
    </div>
  </div>
</body>
</html>